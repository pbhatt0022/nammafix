/**
 * Swap point for generated illustration assets (e.g. Higgsfield).
 * Leave undefined to use the built-in vector SVG. To use a raster hero, drop the
 * file in /assets and set heroImage to its URL — DoodleHero renders it automatically.
 */
export const heroImage: string | undefined = undefined;
